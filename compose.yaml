services:
  postgres:
    image: postgres:16-alpine
    container_name: projet-indiv26-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: projet_indiv26
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app_local_change_me
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - projet_indiv26_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d projet_indiv26"]
      interval: 5s
      timeout: 3s
      retries: 10

  keycloak:
    image: quay.io/keycloak/keycloak:26.7.4
    container_name: projet-indiv26-keycloak
    restart: unless-stopped
    command: ["start-dev", "--import-realm"]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin_local_change_me
    ports:
      - "127.0.0.1:8081:8080"
    volumes:
      - ./infra/keycloak/projet-indiv26-realm.json:/opt/keycloak/data/import/projet-indiv26-realm.json:ro
      - ./infra/keycloak/themes/petite-maison:/opt/keycloak/themes/petite-maison:ro
    mem_limit: 1g

volumes:
  projet_indiv26_pgdata:
